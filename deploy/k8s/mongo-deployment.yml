apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-deployment
  labels:
    app: todo-app
spec:
  selector:
    matchLabels:
      app: todo-app
  type: Standalone
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      containers:
      - name: mongo
        image: mongo:4.2
        args:
          - --bind_ip
          - 0.0.0.0
          - "--port"
          - "27017"
        ports:
        - containerPort: 27017
      dnsPolicy: Default